import { SearchBox } from "@/components/SearchBox"; import { SongCard } from "@/components/SongCard"; import { AdSlot } from "@/components/AdSlot"; import { getSongs } from "@/lib/api"; import Link from "next/link";
export default async function Home(){const songs=await getSongs();return <div className="page"><section className="hero"><span className="eyebrow">A quieter place to worship</span><h1>Find the song.<br/><em>Sing the truth.</em></h1><p>Christian lyrics in Hindi, Nepali, and English — made easy to find, easy to read, and easy to carry with you.</p><SearchBox/><div className="language-links"><Link href="/hindi">हिन्दी lyrics →</Link> <Link href="/nepali">नेपाली lyrics →</Link> <Link href="/english">English lyrics →</Link></div></section><AdSlot placement="homepage_banner"/><section className="section"><div className="section-head"><div><span className="eyebrow">The collection</span><h2>Recently added</h2></div><span className="muted">{songs.length} songs</span></div><div className="song-grid">{songs.map(s=><SongCard key={s.id} song={s}/>)}</div></section></div>}
