import { getSongs } from "@/lib/api"; import { Language } from "@/lib/types"; import { SongCard } from "@/components/SongCard"; import { notFound } from "next/navigation"; import { AdSlot } from "@/components/AdSlot";
export default async function LanguagePage({params}:{params:Promise<{language:string}>}){const {language}=await params;if(!["hindi","nepali","english"].includes(language))notFound();const lang=language as Language;const songs=await getSongs(lang);const label=lang === "hindi" ? "Hindi / हिन्दी" : lang === "nepali" ? "Nepali / नेपाली" : "English";return <div className="page"><section className="language-banner"><span className="eyebrow">Song collection</span><h1>{label} lyrics</h1><p>Find songs in {label.toLowerCase()}, thoughtfully formatted for reading and singing.</p></section><AdSlot placement={`${lang}_category`}/><section className="section"><div className="section-head"><h2>All songs</h2><span className="muted">{songs.length} songs</span></div><div className="song-grid">{songs.map(s=><SongCard key={s.id} song={s}/>)}</div></section></div>}
